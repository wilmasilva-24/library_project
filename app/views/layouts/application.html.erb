<!DOCTYPE html>
<html>
  <head>
    <title>LibraryProject</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <div class="container-fluid">
        <a class="navbar-brand" href="#">Sistema Biblioteca</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item">
              <% if current_user %>
                <%= link_to 'Reservas', reservations_path,  class: "nav-link active"  %>
              <% end %>
            </li>
            <li class="nav-item">
              <% if current_user %>
                <%= link_to 'Livros', books_path,  class: "nav-link active"  %>
              <% end %>
            </li>
            <li class="nav-item">
              <% if current_user && policy(:user).create? %>
                <%= link_to 'Usuários', users_path,  class: "nav-link active"  %>
              <% end %>
            </li>
          </ul>
          <form class="d-flex">
            <%= link_to 'Deslogar do sistema', destroy_user_session_path, method: :delete,  class: "btn btn-outline-success" if current_user %>
          </form>
        </div>
      </div>
    </nav>
    <div class="container">
      <%= flash[:notice] %>
      <%= yield %>
    </div>
  </body>
</html>
